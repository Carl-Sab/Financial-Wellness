[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "eurisko-combined-prediction"
version = "0.1.0"
description = "Combined Eurisko arousal, impulse, and overspending prediction pipeline"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
namespaces = true
